<a href="#content" class="skiplink">Skip to main content</a>

{#
<!--[if IE]>
    <div class="inner">
        <p class="browserupgrade">You are using an <strong>outdated</strong> browser. Please <a href="https://browsehappy.com/">upgrade your browser</a> to improve your experience and security.</p>
    </div>
<![endif]-->
#}

<header>
    <div class="inner">
        {% if page.url == '/' %}
            <h1>{{ general.site_name }}</h1>
        {% else %}
            <a href="/" class="sitename">{{ general.site_name }}</span></a>
        {% endif %}
    </div>
</header>

<nav>
    <div class="inner">
        {# <button aria-expanded="false" aria-controls="main-menu">Menu</button> #}
        <ul id="main-menu" class="headerlinks" {# hidden #}>
            {% for url, label in general.menu %}
                <li{% if page.url == url or (general.parent[ type ] is defined and url == general.parent[ type ].url) %} class="active"{% endif %}>
                    <a href="{{ url | url }}">
                        {{- label -}}
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div>
</nav>
