---
layout: layouts/base.njk
title: Monster Hunter World Leaderboards
---

{#
<ul>
{% for monster in collections.monsters %}
    <li><a href="{{ monster }}">{{ monster.title }}</a></li>
{% endfor  %}
<ul>
#}

<h2>Locations</h2>
<ul>
{% for item in collections.locations %}
    <li
        {% if page.url == item.url %} class="active"{% endif %}>
        <a href="{{ item.url | url }}">
            {{ item.data.title }}
        </a>
        {#
        [{{ item.data.title | slug }}]
        [{{ item.fileSlug }}]
        #}

        {# --- hmm.... a bit overkill, but only way to get the _related_ data --- #}
        {#
        {{ collections.lookup.location[ item.fileSlug ].data.title }}
        #}
    </li>
{% endfor  %}
</ul>

<h2>Quests</h2>
<ul>
{% for item in collections.quests %}
    {# -- quests are sub-divided per quest type -- #}
    <li
        {% if page.url == item.url %} class="active"{% endif %}>
        <a href="{{ item.url | url }}">
            {{ item.data.title }}
        </a>
    </li>
{% endfor  %}
</ul>

<h2>Monsters</h2>
<ul>
{% for item in collections.monsters %}
    <li
        {% if page.url == item.url %} class="active"{% endif %}>
        <a href="{{ item.url | url }}">
            {{ item.data.title }}
        </a>
    </li>
{% endfor  %}
</ul>

<h2>Weapons</h2>
<ul>
{% for item in collections.weapons %}
    <li
        {% if page.url == item.url %} class="active"{% endif %}>
        <a href="{{ item.url | url }}">
            {{- item.data.title -}}
        </a>
    </li>
{% endfor  %}
</ul>

<h2>Runners</h2>
<ul>
{% for item in collections.runners %}
    <li
        {% if page.url == item.url %} class="active"{% endif %}>
        <a href="{{ item.url | url }}">
            {{- item.data.title -}}
        </a>
        {#
        {% for weapon in item.data.weapons %}
            <span class="weapon-icon weapon-icon-{{ weapon }}"></span>
        {% endfor %}
        #}
        {% if item.data.country %}
            <span class="flag-icon flag-icon-{{ item.data.country }}"></span>
        {% endif %}
    </li>
{% endfor  %}
</ul>

<h2>Runs</h2>
<ul>
{% for item in collections.runs %}
    <li
        {% if page.url == item.url %} class="active"{% endif %}>
        <a href="{{ item.url | url }}">
            {{- item.data.title -}}
        </a>
    </li>
{% endfor  %}
</ul>

<h3>Test Times</h3>
<pre>
      999     {{       999 | millisFormatted }}
     1000     {{      1000 | millisFormatted }}
    10000     {{     10000 | millisFormatted }}
    65000     {{     65000 | millisFormatted }}
   100000     {{    100000 | millisFormatted }}
  1000000     {{   1000000 | millisFormatted }}
 10000000     {{  10000000 | millisFormatted }}
100000000     {{ 100000000 | millisFormatted }}
</pre>
